{
  const d = {
    height: 180,
    weight: 78,
    weight: 84,
    temperature: 36,
    eyesight: 1,
  };
  console.log(d["weight"]);

  /**
   * JS 키의 중복의 경우, 가장 마지막 이름의 식별자 값으로 결정한다.
   */
}
