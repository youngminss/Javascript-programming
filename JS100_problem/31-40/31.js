// Array 내장함수 시간복잡도 O(1) 아닌 것
{
  // Array[i]
  // Array.prototype.push(...items)
  // Array.prototype.slice(start, end)	// X
  // Array.prototype.pop()
  // Array.prototype.includes(searchElement, fromIndex)	// X
}
